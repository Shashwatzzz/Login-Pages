# Login-Pages
This project is combination of clone user login pages of spotify,facebook and netflix.<br>

The project contains three major repositories Facebook, Spotify and Netflix.<br>
Each representing respective login page.<br>

<h1>Running and testing the project files.</h1><br>

<h4>For running and testing the project we can either use the traditional port forwarding method or we can use a prebuilt live server. </h4>
<h2>Downloading the project in local host.</h2><br>
<h3>To get the git project in local host.</h3><br>
<h4>Use command git clone < link of the project > in the command prompt or code editor terminal</h4>
<h2>Launching the project in local host.</h2>
After the installation of project in local host we can either use prot forwarding or live server. I have used the live server to launch the project.

<h4>When we will launch the project through go live option from live server extension the first the webpage will  look like this. Because by convention the live server lokks for 
< index.html >file as the home page of the website.</h4>
![Screenshot (1848)](https://github.com/user-attachments/assets/f0f1d559-c5a8-4246-b4fa-0caf50d4eb32)
<h4>In the can either select any of the folder then you can the .html file to open the login menu but it is a bad option.</h4>
<h4>Insted you can play with the URL above to get the desired result.</h4>
<h3>Step 1</h3>
<h4>You can modify the URL name on the with respective login page name and then html file like <br> appname/appname.html </h4>
![Screenshot (1846)](https://github.com/user-attachments/assets/d72f7bcb-b684-4607-a5b4-92121eca70d4)
<h2>You can check the pages for all three facebook, spotify and netflix.</h2>

Project made by Shahswat Tripathi
